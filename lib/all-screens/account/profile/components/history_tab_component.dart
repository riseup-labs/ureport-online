import 'package:flutter/material.dart';

class HistoryTabComponent extends StatelessWidget {
  const HistoryTabComponent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      margin: EdgeInsets.only(top: 20),
      color: Colors.red,
    );
  }
}
